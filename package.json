{
  "name": "chrona.service.backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "vercel dev",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run smoke",
    "smoke": "npx tsx scripts/smoke-app-fetch.ts",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@apple/app-store-server-library": "^3.0.0",
    "@supabase/supabase-js": "^2.105.3",
    "hono": "^4.7.0",
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vercel": "^53.1.0"
  }
}
